{
  "name": "web-admin-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.html",
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "rm -rf ${BUILD_DIR:-build} && parcel build ./src/index.html --no-source-maps -d ${BUILD_DIR:-build}",
    "start:prod": "node serve.js",
    "nodemon": "nodemon serve.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "parcel-bundler": "^1.12.5",
    "posthtml-include": "^1.7.2",
    "regenerator-runtime": "^0.13.9"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
